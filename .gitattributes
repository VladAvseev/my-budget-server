# Нормализ переводов строк: checkout обязано быть LF — иначе sha256-хэш
# текста юридического документа зависел бы от autocrlf машины, а диффы
# docs/legal светили бы перекодировку всего файла. CLI нормализует CRLF→LF
# при публикации, но чище держать в индексе сразу LF.
* text=auto eol=lf
